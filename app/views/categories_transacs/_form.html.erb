<%= form_with(model: categories_transac) do |form| %>
  <% if categories_transac.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categories_transac.errors.count, "error") %> prohibited this categories_transac from being saved:</h2>

      <ul>
        <% categories_transac.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :transacs_id, style: "display: block" %>
    <%= form.text_field :transacs_id %>
  </div>

  <div>
    <%= form.label :categories_id, style: "display: block" %>
    <%= form.text_field :categories_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
